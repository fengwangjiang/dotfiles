input=$1
strinput=${input%.tex}

cp /tmp/${strinput}.bbl .
pdflatex -interaction nonstopmode -output-directory /tmp --shell-escape $input | grep -i error

if [ -z "$2" ]; then
    xdg-open /tmp/${input%.tex}.pdf > /dev/null 2>&1
fi
